# Sensitivity Taxonomy

Allowed sensitivity values:
- `public`
- `internal`
- `confidential`
- `restricted`

Rules:
- `public` may be shared externally if publish contract allows it.
- `internal` may be shared inside the company after review.
- `confidential` requires explicit review and limited visibility.
- `restricted` requires explicit approval and must not be automated by default.

