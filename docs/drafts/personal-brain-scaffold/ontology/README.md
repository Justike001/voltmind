# Ontology

Control-plane ontology files for shared company-brain vocabulary and lens coordination.

Use `ontology/core.yaml` for company-wide primitives and `ontology/lenses/` for department-specific extensions.
