# VoltMind 维护复核报告（2026-07-17）

## 本轮已完成

- 已清理两个历史噪声人物页（`people/primary-home`、`people/open-thread`），并排除它们对活动库健康指标的干扰。
- 修正活动库健康、图谱覆盖和完整性批量扫描对软删除页面的计数：仅活动页面、活动关系参与指标。
- 完整性全量扫描：86 个活动页面，0 条 bare-tweet，0 条外链待检查。
- 补齐 8 个人物页的 28 条已验证反向引用，并同步、全量抽取及重嵌入。
- 已执行受控完整周期并修复 `dream --source <id>` 未写入周期完成时间的缺陷；`default` 的周期新鲜度现为正常。

## 当前结果

| 指标 | 当前值 | 结论 |
| --- | ---: | --- |
| 总健康分 | 70 | 警告态，但 P0 周期问题已消除 |
| 脑库分 | 74/100 | 活动内容质量正常，评分口径仍包含导航/模板页 |
| 活动页面 | 86 | 单一 `default` 来源 |
| 嵌入覆盖 | 100% | 0 个 stale chunks |
| 实体链接覆盖 | 100% | 已满足目标 |
| 实体时间线覆盖 | 100% | 已满足目标 |
| 完整性 | 0 bare-tweet / 0 外链 | 已清零 |

## 仍需处理的项目

1. **全页脑库评分口径**
   - 分项仍为：时间线 3/15、孤儿页 7/15、死链 4/10。
   - 原因不是实体覆盖不足：评分把 README、索引、政策、模板等 86 个活动页面一并计入；其中 45 个页面没有图关系，且大量页面不应拥有业务时间线。
   - 建议：将导航、政策、模板和目录说明页从“内容页时间线/孤儿”计分口径排除，而不要为抬分数虚构链接或时间线。

2. **软删除历史关系**
   - 仍有指向软删除页面的旧关系，故死链项为 4/10。
   - 建议先输出关系明细并确认；确认后仅删除指向已清理历史页面的关系。不要通过新增页面或恢复历史噪声来消除该告警。

3. **Lint：70 项 / 64 页**
   - 主要是 README、目录索引、政策和架构文件没有 YAML frontmatter；它们通常不应进入内容质量计分。
   - 21 个内容页面缺 `created`；其中 2 篇日报也缺 `title`。
   - 3 个模板含预期的 `YYYY-MM-DD` 占位日期。
   - 建议分两步处理：先给真实内容页补元数据；再让 lint 跳过 README/模板/政策类文档或为其采用轻量 frontmatter 规范。

4. **非内容运行时警告**
   - resolver 路由中有 40 个孤立触发器；需整理 `skills/RESOLVER.md` 与 `skills/manifest.json`。
   - reranker 近 7 天有一次认证失败；需核验 `ZEROENTROPY_API_KEY` 后运行 `voltmind models doctor`。
   - `whoknows` 缺少评测 fixture 路径；配置 `VOLTMIND_WHOKNOWS_FIXTURE_PATH` 后可消除。
   - 对话解析器的 2 个对话页未匹配内置格式；如需自动抽取，应逐页检查格式或启用 LLM fallback。

## 已验证的实现变更

- `getHealth()` 与 doctor 图谱统计均忽略软删除页面。
- 完整性批量扫描仅检查活动页面。
- `dream --source <id>` 会将成功周期写入来源的 `last_full_cycle_at`。
- 针对脑库分的回归测试通过；CLI 二进制已重新构建。

## 下一步建议

先确认“软删除历史关系”的明细和“内容页元数据补齐”范围。两项确认后，可继续将全页评分与实际知识库质量对齐，而不是用噪声链接抬分。
